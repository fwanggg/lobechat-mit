// Family-OS: the right hand panel is gone. Topics live in the left column now, and the
// role-setting block that used to sit above them has no meaning for a one-agent household.
const Topic = () => null;

Topic.displayName = 'ChatTopic';

export default Topic;
